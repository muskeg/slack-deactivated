{
  "name": "slack-deactivated",
  "private": true,
  "version": "1.0.0",
  "description": "Generate Slack-style deactivated account profile pictures",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.1.0"
  }
}
